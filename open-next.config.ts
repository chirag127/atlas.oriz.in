import { defineCloudflareConfig } from "@opennextjs/cloudflare/config";

export default defineCloudflareConfig({
  // Default OpenNext Cloudflare configuration
  // Customize as needed for production deployment
});
